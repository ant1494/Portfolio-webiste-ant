<section class="hero">
  <img class="hero__image" src="{{ settings.hero_image }}" alt="" loading="eager">
  <div class="hero__overlay">
    <p class="hero__eyebrow">Portfolio</p>
    <h1 class="hero__title">{{ settings.artist_name }}</h1>
    <p class="hero__tagline">{{ settings.tagline }}</p>
  </div>
</section>
